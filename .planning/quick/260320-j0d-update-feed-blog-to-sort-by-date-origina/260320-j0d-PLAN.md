---
phase: quick-260320-j0d-update-feed-blog-to-sort-by-date-origina
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/content/models.py
  - apps/content/tests/__init__.py
  - apps/content/tests/test_blog_index_ordering.py
  - apps/bluesky/sync.py
  - apps/bluesky/publish.py
  - apps/bluesky/tests/test_sync_bluesky_command.py
autonomous: true
requirements:
  - QUICK-260320-J0D
must_haves:
  truths:
    - "Blog/feed entries render in chronological order by the post's own content date, not by sync/publish timestamp."
    - "Imported Bluesky microposts store the original Bluesky post date so feed ordering remains stable when backfilling older posts."
    - "Re-syncing an existing Bluesky post does not move it to the top unless its original posted date changed."
  artifacts:
    - path: "apps/content/models.py"
      provides: "BlogIndex ordering by content date with safe fallback for pages without date"
      contains: "get_ordered_blog_posts"
    - path: "apps/bluesky/sync.py"
      provides: "Timestamp extraction that prefers Bluesky createdAt (original posted time)"
      contains: "createdAt"
    - path: "apps/bluesky/publish.py"
      provides: "MicroPostPage.date assignment from original posted timestamp"
      contains: "micro_post.date"
    - path: "apps/content/tests/test_blog_index_ordering.py"
      provides: "Regression coverage for date-based feed ordering"
      contains: "TestCase"
  key_links:
    - from: "apps/bluesky/sync.py"
      to: "apps/bluesky/publish.py"
      via: "upsert_and_publish_micro_post timestamp argument"
      pattern: "upsert_and_publish_micro_post"
    - from: "apps/bluesky/publish.py"
      to: "apps/content/models.py"
      via: "MicroPostPage.date consumed by BlogIndexPage.get_ordered_blog_posts"
      pattern: "date="
---

<objective>
Make blog/feed ordering deterministic by original post date so Bluesky backfills appear where they were originally published instead of where they were synced.

Purpose: Prevent newly synced historical posts from jumping to the top of the feed due to Wagtail publish timestamps.
Output: Date-based ordering logic, original Bluesky timestamp mapping, and regression tests.
</objective>

<context>
@.planning/STATE.md
@apps/content/models.py
@apps/bluesky/sync.py
@apps/bluesky/publish.py
@apps/bluesky/tests/test_sync_bluesky_command.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add regression tests for feed ordering and timestamp preference</name>
  <files>apps/content/tests/__init__.py, apps/content/tests/test_blog_index_ordering.py, apps/bluesky/tests/test_sync_bluesky_command.py</files>
  <behavior>
    - Test 1: Blog index returns newer `date` entries before older ones even if an older post was published/synced later.
    - Test 2: Sync uses Bluesky `value.createdAt` as the post timestamp when both `indexedAt` and `createdAt` exist.
    - Test 3: Sync still falls back to `indexedAt` when `createdAt` is missing.
  </behavior>
  <action>Create content-level regression tests that fail under current `-first_published_at` ordering and current timestamp selection priority. Keep fixtures minimal: one `BlogIndexPage` with mixed child posts and explicit `date` values, plus sync contract tests that assert which timestamp reaches publish layer.</action>
  <verify>
    <automated>make test ARGS='apps.content.tests.test_blog_index_ordering apps.bluesky.tests.test_sync_bluesky_command -x --keepdb'</automated>
  </verify>
  <done>Failing tests clearly encode the expected behavior: feed order uses post date and Bluesky date mapping prefers original posted time.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement original-posted timestamp mapping in Bluesky sync/publish path</name>
  <files>apps/bluesky/sync.py, apps/bluesky/publish.py</files>
  <behavior>
    - Test 1: For imported records with both fields, `createdAt` is selected and used to set `MicroPostPage.date`.
    - Test 2: Records without `createdAt` continue to import using `indexedAt` fallback.
  </behavior>
  <action>Update record timestamp extraction to prefer `value.createdAt` over `indexedAt` (original posted-time first, index-time fallback). Pass this value through to publish/update and continue storing/updating `MicroPostPage.date` from that selected timestamp so backfills preserve historical chronology.</action>
  <verify>
    <automated>make test ARGS='apps.bluesky.tests.test_sync_bluesky_command apps.bluesky.tests.test_publish -x --keepdb'</automated>
  </verify>
  <done>Imported and updated Bluesky micropost pages carry the original post date, with backward-compatible fallback when `createdAt` is unavailable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Switch BlogIndex ordering from publish timestamp to content date</name>
  <files>apps/content/models.py, apps/content/tests/test_blog_index_ordering.py</files>
  <behavior>
    - Test 1: `get_ordered_blog_posts()` sorts all blog-feed child pages by their `date` descending.
    - Test 2: Pages lacking `date` do not break ordering and are placed using a deterministic fallback (e.g., `first_published_at`).
  </behavior>
  <action>Replace `-first_published_at` ordering in `BlogIndexPage.get_ordered_blog_posts()` with date-first ordering aligned to feed semantics. Because this queryset returns polymorphic page types, implement a deterministic strategy that works across child page types (all current feed pages expose `date`) and preserves stability with a fallback timestamp for any legacy/no-date entries.</action>
  <verify>
    <automated>make test ARGS='apps.content.tests.test_blog_index_ordering -x --keepdb'</automated>
  </verify>
  <done>Feed retrieval logic is date-driven and verified against the sync-backfill regression scenario.</done>
</task>

</tasks>

<verification>
- `make test ARGS='apps.content.tests.test_blog_index_ordering -x --keepdb'`
- `make test ARGS='apps.bluesky.tests.test_sync_bluesky_command apps.bluesky.tests.test_publish -x --keepdb'`
</verification>

<success_criteria>
- Syncing older Bluesky posts no longer moves them to the top of `/blog/`.
- Feed order reflects original post chronology using page `date` values.
- Test coverage protects both ordering logic and timestamp-selection contract.
</success_criteria>

<output>
After completion, create `.planning/quick/260320-j0d-update-feed-blog-to-sort-by-date-origina/260320-j0d-SUMMARY.md`
</output>
