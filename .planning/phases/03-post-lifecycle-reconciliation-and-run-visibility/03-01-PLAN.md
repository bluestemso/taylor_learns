---
phase: 03-post-lifecycle-reconciliation-and-run-visibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/bluesky/tests/test_sync_lifecycle_reconciliation.py
  - apps/bluesky/tests/test_reconcile.py
  - apps/bluesky/models.py
  - apps/bluesky/migrations/0003_blueskypostmap_removed_at.py
  - apps/bluesky/reconcile.py
  - apps/bluesky/publish.py
  - apps/bluesky/sync.py
autonomous: true
requirements:
  - LIFE-01
  - LIFE-02
must_haves:
  truths:
    - "Edited Bluesky posts update the already-mirrored microblog entry after sync."
    - "Deleted Bluesky posts become unpublished in Wagtail after sync."
    - "A previously removed post is not repeatedly counted as removed on every run."
  artifacts:
    - path: "apps/bluesky/reconcile.py"
      provides: "Deterministic helpers for identifying update/remove candidates from URI/CID state"
      contains: "classify_record_operation"
    - path: "apps/bluesky/publish.py"
      provides: "Soft-remove operation using Wagtail unpublish for mapped micro posts"
      contains: "unpublish"
    - path: "apps/bluesky/models.py"
      provides: "Persistent lifecycle marker for removed mapped records"
      contains: "removed_at"
    - path: "apps/bluesky/sync.py"
      provides: "Two-pass lifecycle reconciliation with remove pass and removed counter"
      contains: "removed"
  key_links:
    - from: "apps/bluesky/sync.py"
      to: "apps/bluesky/reconcile.py"
      via: "operation classification and missing URI reconciliation"
      pattern: "classify_record_operation|missing"
    - from: "apps/bluesky/sync.py"
      to: "apps/bluesky/publish.py"
      via: "upsert on created/updated and unpublish on removed"
      pattern: "upsert_and_publish_micro_post|unpublish"
    - from: "apps/bluesky/models.py"
      to: "apps/bluesky/sync.py"
      via: "removed_at guards repeat removals"
      pattern: "removed_at"
---

<objective>
Deliver deterministic lifecycle parity so sync updates edited source posts and soft-removes deleted source posts without repeated remove churn.

Purpose: Satisfies LIFE-01 and LIFE-02 by extending the existing URI/CID reconciliation architecture instead of replacing it.
Output: Lifecycle reconciliation tests, post-map removal metadata, and two-pass sync remove behavior.
</objective>

<execution_context>
@/Users/tschaack/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/tschaack/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-deterministic-import-and-auto-publish/02-03-SUMMARY.md
@apps/bluesky/sync.py
@apps/bluesky/reconcile.py
@apps/bluesky/publish.py
@apps/bluesky/models.py

<interfaces>
From apps/bluesky/reconcile.py:
```python
def classify_record_operation(*, source_uri: str, source_cid: str) -> str:
    ...
```

From apps/bluesky/publish.py:
```python
def upsert_and_publish_micro_post(
    *,
    source_settings,
    source_uri: str,
    source_cid: str,
    source_did: str,
    source_rkey: str,
    source_indexed_at,
    post_text: str,
    post_facets: list[dict] | None,
) -> dict[str, str]:
    ...
```

From apps/bluesky/sync.py:
```python
def run_sync(*, limit: int = 100) -> dict[str, int]:
    ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create failing lifecycle reconciliation tests for edits and removals</name>
  <read_first>
    - apps/bluesky/tests/test_sync_bluesky_command.py
    - apps/bluesky/tests/test_reconcile.py
    - apps/bluesky/sync.py
  </read_first>
  <files>apps/bluesky/tests/test_sync_lifecycle_reconciliation.py, apps/bluesky/tests/test_reconcile.py</files>
  <acceptance_criteria>
    - New tests cover: CID change updates mapped micro post body (LIFE-01).
    - New tests cover: mapped URI missing from remote set triggers soft-remove flow (LIFE-02).
    - At least one new test fails before implementation when run in isolation.
  </acceptance_criteria>
  <action>
    Add `apps/bluesky/tests/test_sync_lifecycle_reconciliation.py` with deterministic contract tests for: (1) edit reconciliation using same URI + changed CID and (2) deletion reconciliation where existing mapped URI is absent from remote records and should be unpublished. Update `apps/bluesky/tests/test_reconcile.py` with helper-level tests for missing-URI candidate selection to anchor delete detection behavior.
  </action>
  <verify>
    <automated>make test ARGS='apps.bluesky.tests.test_sync_lifecycle_reconciliation apps.bluesky.tests.test_reconcile'</automated>
  </verify>
  <done>Lifecycle reconciliation tests exist, are deterministic, and include RED coverage for missing implementation paths.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement removal state contracts and soft-remove publish operation</name>
  <read_first>
    - apps/bluesky/models.py
    - apps/bluesky/publish.py
    - apps/content/models.py
  </read_first>
  <files>apps/bluesky/models.py, apps/bluesky/migrations/0003_blueskypostmap_removed_at.py, apps/bluesky/publish.py, apps/bluesky/reconcile.py</files>
  <acceptance_criteria>
    - `BlueskyPostMap` persists nullable removal metadata via `removed_at`.
    - Publish layer exposes a callable soft-remove operation that unpublishes mapped micro posts.
    - Reconcile helpers can identify missing mapped URIs while excluding already removed rows.
  </acceptance_criteria>
  <action>
    Add `removed_at = models.DateTimeField(null=True, blank=True)` to `BlueskyPostMap` and create migration `0003_blueskypostmap_removed_at.py`. In `apps/bluesky/publish.py`, add a dedicated soft-remove function that loads the mapped micro post, calls `micro_post.unpublish(set_expired=False, log_action=True)`, and stamps `removed_at` when first removed. In `apps/bluesky/reconcile.py`, add helper(s) that compute deletion candidates from `source_settings` map URIs minus fetched remote URIs, excluding rows where `removed_at` is already set.
  </action>
  <verify>
    <automated>make test ARGS='apps.bluesky.tests.test_sync_lifecycle_reconciliation apps.bluesky.tests.test_publish apps.bluesky.tests.test_reconcile'</automated>
  </verify>
  <done>Model, migration, and publish/reconcile lifecycle helpers are implemented and tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire two-pass run_sync reconciliation with removed counter</name>
  <read_first>
    - apps/bluesky/sync.py
    - apps/bluesky/client.py
    - apps/bluesky/reconcile.py
    - apps/bluesky/publish.py
  </read_first>
  <files>apps/bluesky/sync.py, apps/bluesky/client.py, apps/bluesky/reconcile.py, apps/bluesky/publish.py, apps/bluesky/tests/test_sync_lifecycle_reconciliation.py</files>
  <acceptance_criteria>
    - Sync exhausts paginated `listRecords` pages before computing deletion candidates.
    - Result counters include `imported`, `updated`, `removed`, `skipped`, and `failed`.
    - Removed rows are counted once per removal event (no repeated increment while still absent).
  </acceptance_criteria>
  <action>
    Extend sync orchestration to use two passes: first pass iterates all fetched records across cursor pages for create/update/skip decisions; second pass computes missing mapped URIs and calls the new soft-remove operation. Keep existing classify-before-publish behavior for created/updated records. Update in-memory counters to include `removed` as a first-class key and preserve `failed` diagnostics. Ensure fetch behavior exhausts cursors (max per request 100) so delete detection uses complete remote URI state.
  </action>
  <verify>
    <automated>make test ARGS='apps.bluesky.tests.test_sync_lifecycle_reconciliation apps.bluesky.tests.test_sync_bluesky_command'</automated>
  </verify>
  <done>run_sync deterministically reconciles edits and deletions with complete pagination and returns counters including removed.</done>
</task>

</tasks>

<verification>
- `make test ARGS='apps.bluesky.tests.test_sync_lifecycle_reconciliation apps.bluesky.tests.test_reconcile apps.bluesky.tests.test_publish apps.bluesky.tests.test_sync_bluesky_command'`
</verification>

<success_criteria>
- Edited source records update the mapped live micro post on the next sync.
- Missing source URIs trigger one-time soft-removal (unpublish) of mapped micro posts.
- Sync counter contract includes `removed` without regressing imported/updated/skipped behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/03-post-lifecycle-reconciliation-and-run-visibility/03-01-SUMMARY.md`
</output>
