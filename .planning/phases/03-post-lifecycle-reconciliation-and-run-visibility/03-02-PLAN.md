---
phase: 03-post-lifecycle-reconciliation-and-run-visibility
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - apps/bluesky/tests/test_sync_run_visibility.py
  - apps/bluesky/tests/test_sync_bluesky_command.py
  - apps/bluesky/models.py
  - apps/bluesky/migrations/0004_blueskysyncrun.py
  - apps/bluesky/admin.py
  - apps/bluesky/sync.py
  - apps/bluesky/management/commands/sync_bluesky.py
autonomous: true
requirements:
  - LIFE-03
must_haves:
  truths:
    - "Operator can inspect each sync run with imported, updated, removed, and skipped counts."
    - "Sync command output includes the same counters as persisted run records."
    - "Run history remains queryable in admin with source and completion timestamps."
  artifacts:
    - path: "apps/bluesky/models.py"
      provides: "Persistent run history model with deterministic counters"
      contains: "class BlueskySyncRun"
    - path: "apps/bluesky/admin.py"
      provides: "Admin visibility for run records"
      contains: "@admin.register(BlueskySyncRun)"
    - path: "apps/bluesky/sync.py"
      provides: "Run persistence created from returned counters"
      contains: "BlueskySyncRun.objects.create"
    - path: "apps/bluesky/management/commands/sync_bluesky.py"
      provides: "Operator output including removed count"
      contains: "removed="
  key_links:
    - from: "apps/bluesky/sync.py"
      to: "apps/bluesky/models.py"
      via: "create run record at end of orchestration"
      pattern: "BlueskySyncRun.objects.create"
    - from: "apps/bluesky/management/commands/sync_bluesky.py"
      to: "apps/bluesky/sync.py"
      via: "command renders persisted counter contract"
      pattern: "run_sync"
    - from: "apps/bluesky/admin.py"
      to: "apps/bluesky/models.py"
      via: "register and list run model fields"
      pattern: "BlueskySyncRun"
---

<objective>
Provide durable run visibility so each Bluesky sync has an inspectable outcome record and consistent operator-facing counter output.

Purpose: Satisfies LIFE-03 with persisted run history and deterministic imported/updated/removed/skipped visibility.
Output: Run history model + admin UI + sync/command wiring and tests.
</objective>

<execution_context>
@/Users/tschaack/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/tschaack/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-post-lifecycle-reconciliation-and-run-visibility/03-01-PLAN.md
@apps/bluesky/models.py
@apps/bluesky/sync.py
@apps/bluesky/management/commands/sync_bluesky.py
@apps/bluesky/admin.py

<interfaces>
From apps/bluesky/sync.py (post-03-01 expected counter contract):
```python
def run_sync(*, limit: int = 100) -> dict[str, int]:
    # returns imported, updated, removed, skipped, failed
    ...
```

From apps/bluesky/management/commands/sync_bluesky.py:
```python
result = run_sync(limit=int(options.get("limit") or 100))
```

From apps/bluesky/admin.py:
```python
@admin.register(BlueskySourceSettings)
class BlueskySourceSettingsAdmin(admin.ModelAdmin):
    ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing run-visibility tests for model persistence and command output</name>
  <read_first>
    - apps/bluesky/tests/test_sync_bluesky_command.py
    - apps/bluesky/sync.py
    - apps/bluesky/admin.py
  </read_first>
  <files>apps/bluesky/tests/test_sync_run_visibility.py, apps/bluesky/tests/test_sync_bluesky_command.py</files>
  <acceptance_criteria>
    - New tests assert a run record is created per sync with imported/updated/removed/skipped counters.
    - New tests assert command output string includes `removed=` with deterministic key order.
    - At least one new run-visibility test fails before implementation.
  </acceptance_criteria>
  <action>
    Create `apps/bluesky/tests/test_sync_run_visibility.py` covering run-row persistence and required counter fields. Update `apps/bluesky/tests/test_sync_bluesky_command.py` expected output contract to `imported updated removed skipped failed` so missing `removed` is caught. Include one admin-facing assertion (queryset/list display-level) that verifies run history is inspectable.
  </action>
  <verify>
    <automated>make test ARGS='apps.bluesky.tests.test_sync_run_visibility apps.bluesky.tests.test_sync_bluesky_command'</automated>
  </verify>
  <done>Run visibility contracts are encoded in tests and initially RED for new persistence/output behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement BlueskySyncRun model, migration, and admin registration</name>
  <read_first>
    - apps/bluesky/models.py
    - apps/bluesky/admin.py
    - apps/gadgets/models.py
    - apps/gadgets/admin.py
  </read_first>
  <files>apps/bluesky/models.py, apps/bluesky/migrations/0004_blueskysyncrun.py, apps/bluesky/admin.py</files>
  <acceptance_criteria>
    - `BlueskySyncRun` model exists with source relation and integer counters for imported/updated/removed/skipped/failed.
    - Migration creates the run table and constraints/indexes needed for source-ordered history.
    - Admin exposes run rows with source handle and counter columns.
  </acceptance_criteria>
  <action>
    Add `BlueskySyncRun(BaseModel)` with `source_settings` FK, `started_at`, `completed_at`, and integer counters defaulting to 0 (`imported_count`, `updated_count`, `removed_count`, `skipped_count`, `failed_count`). Add ordering by `-completed_at, -created_at` and create migration `0004_blueskysyncrun.py`. Register the model in `apps/bluesky/admin.py` using list display columns: source, completed_at, imported_count, updated_count, removed_count, skipped_count, failed_count.
  </action>
  <verify>
    <automated>make test ARGS='apps.bluesky.tests.test_sync_run_visibility'</automated>
  </verify>
  <done>Run model schema is migrated and visible in admin with required outcome fields.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Persist run outcomes from sync and align command output contract</name>
  <read_first>
    - apps/bluesky/sync.py
    - apps/bluesky/management/commands/sync_bluesky.py
    - apps/bluesky/tests/test_sync_bluesky_command.py
    - apps/bluesky/tests/test_sync_run_visibility.py
  </read_first>
  <files>apps/bluesky/sync.py, apps/bluesky/management/commands/sync_bluesky.py, apps/bluesky/tests/test_sync_run_visibility.py, apps/bluesky/tests/test_sync_bluesky_command.py</files>
  <acceptance_criteria>
    - `run_sync` writes one `BlueskySyncRun` row per invocation with counters matching returned dict.
    - `sync_bluesky` output includes removed and matches order: imported, updated, removed, skipped, failed.
    - Persisted run counters and command output counters are consistent in tests.
  </acceptance_criteria>
  <action>
    In `apps/bluesky/sync.py`, create a run lifecycle: capture `started_at`, execute reconciliation, capture `completed_at`, persist a `BlueskySyncRun` row tied to active source with the exact counter values from the sync result. Keep return shape for command compatibility and include `removed`. Update `apps/bluesky/management/commands/sync_bluesky.py` success message to print `removed` between `updated` and `skipped`.
  </action>
  <verify>
    <automated>make test ARGS='apps.bluesky.tests.test_sync_run_visibility apps.bluesky.tests.test_sync_bluesky_command apps.bluesky.tests.test_sync_lifecycle_reconciliation'</automated>
  </verify>
  <done>Each sync invocation produces inspectable run history and command output that meets LIFE-03 counter requirements.</done>
</task>

</tasks>

<verification>
- `make test ARGS='apps.bluesky.tests.test_sync_run_visibility apps.bluesky.tests.test_sync_bluesky_command apps.bluesky.tests.test_sync_lifecycle_reconciliation'`
</verification>

<success_criteria>
- Operator can view per-run counters (`imported`, `updated`, `removed`, `skipped`) in Django admin.
- Management command output and persisted run record counters are consistent for the same execution.
- No regression of Phase 2 idempotent rerun behavior while adding run visibility persistence.
</success_criteria>

<output>
After completion, create `.planning/phases/03-post-lifecycle-reconciliation-and-run-visibility/03-02-SUMMARY.md`
</output>
