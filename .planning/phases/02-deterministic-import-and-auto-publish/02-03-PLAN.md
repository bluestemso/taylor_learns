---
phase: 02-deterministic-import-and-auto-publish
plan: 03
type: execute
wave: 3
depends_on: [02-01, 02-02]
files_modified:
  - apps/bluesky/client.py
  - apps/bluesky/reconcile.py
  - apps/bluesky/sync.py
  - apps/bluesky/management/__init__.py
  - apps/bluesky/management/commands/__init__.py
  - apps/bluesky/management/commands/sync_bluesky.py
  - apps/bluesky/tests/test_reconcile.py
  - apps/bluesky/tests/test_sync_bluesky_command.py
autonomous: true
requirements: [SYNC-02, SYNC-04]
must_haves:
  truths:
    - "Operator can run a manual `sync_bluesky` command and import new Bluesky posts into microblog entries."
    - "Rerunning the command against unchanged source records does not create additional microblog entries."
    - "Sync output includes deterministic counters for imported, updated, skipped, and failed records."
  artifacts:
    - path: "apps/bluesky/client.py"
      provides: "Bluesky listRecords fetch client for app.bsky.feed.post"
      contains: "def list_feed_post_records"
    - path: "apps/bluesky/reconcile.py"
      provides: "Deterministic operation decision per source record"
      contains: "def classify_record_operation"
    - path: "apps/bluesky/sync.py"
      provides: "Single run_sync orchestration path"
      contains: "def run_sync"
    - path: "apps/bluesky/management/commands/sync_bluesky.py"
      provides: "Manual command entrypoint for sync"
      contains: "class Command(BaseCommand)"
    - path: "apps/bluesky/tests/test_sync_bluesky_command.py"
      provides: "Command wiring contract tests"
      contains: "class TestSyncBlueskyCommand"
  key_links:
    - from: "apps/bluesky/management/commands/sync_bluesky.py"
      to: "apps/bluesky/sync.py"
      via: "Command delegates directly to run_sync"
      pattern: "run_sync"
    - from: "apps/bluesky/sync.py"
      to: "apps/bluesky/publish.py"
      via: "Create/update/skip publish mutations"
      pattern: "upsert_and_publish_micro_post"
    - from: "apps/bluesky/sync.py"
      to: "apps/bluesky/reconcile.py"
      via: "Idempotent operation classification before publish"
      pattern: "classify_record_operation"
---

<objective>
Wire a complete manual sync path that is deterministic and safe to rerun.

Purpose: Deliver the operator-facing sync command with one orchestration path and idempotent import behavior.
Output: Bluesky client + reconcile + sync orchestrator + command + command/reconcile tests.
</objective>

<execution_context>
@/Users/tschaack/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/tschaack/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-deterministic-import-and-auto-publish/02-RESEARCH.md
@.planning/phases/02-deterministic-import-and-auto-publish/02-VALIDATION.md
@.planning/phases/02-deterministic-import-and-auto-publish/02-02-PLAN.md
@apps/gadgets/sync.py
@apps/gadgets/management/commands/sync_gadgets.py
@apps/gadgets/tests/test_sync_gadgets_command.py
@apps/bluesky/models.py
@apps/bluesky/publish.py

<interfaces>
From `apps/gadgets/management/commands/sync_gadgets.py`:
```python
class Command(BaseCommand):
    def handle(self, *args, **options):
        result = sync_all_gadgets(...)
        self.stdout.write(self.style.SUCCESS("Sync complete: ..."))
```

From `apps/bluesky/publish.py` (from Plan 02-02):
```python
def upsert_and_publish_micro_post(*, source_settings, source_uri, source_cid, source_did, source_rkey, source_indexed_at, post_text, post_facets) -> dict[str, str]
```

From `apps/bluesky/models.py` (from Plan 02-01):
```python
class BlueskyPostMap(BaseModel):
    source_uri = models.CharField(max_length=512, unique=True)
    source_cid = models.CharField(max_length=128)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Bluesky record client and reconcile decision logic</name>
  <read_first>
    - apps/bluesky/models.py
    - apps/bluesky/client.py
    - apps/bluesky/reconcile.py
    - apps/bluesky/tests/test_reconcile.py
    - apps/gadgets/sync.py
    - .planning/phases/02-deterministic-import-and-auto-publish/02-RESEARCH.md
  </read_first>
  <files>apps/bluesky/client.py, apps/bluesky/reconcile.py, apps/bluesky/tests/test_reconcile.py</files>
  <behavior>
    - Test 1: `list_feed_post_records` calls `com.atproto.repo.listRecords` with `repo=<source.did>`, `collection="app.bsky.feed.post"`, and forwards `cursor`/`limit`.
    - Test 2: `classify_record_operation` returns `"created"` when no `BlueskyPostMap` exists for `source_uri`.
    - Test 3: `classify_record_operation` returns `"updated"` when map exists and incoming `source_cid` differs.
    - Test 4: `classify_record_operation` returns `"skipped"` when map exists and incoming `source_cid` matches.
  </behavior>
  <action>
    Create `apps/bluesky/client.py` with `LIST_RECORDS_URL = "https://public.api.bsky.app/xrpc/com.atproto.repo.listRecords"` and function:
    `def list_feed_post_records(*, source_settings, cursor: str | None = None, limit: int = 100) -> dict[str, object]`.
    This function must call `httpx.get` with params exactly `{ "repo": source_settings.did, "collection": "app.bsky.feed.post", "limit": limit, "cursor": cursor }` (exclude `cursor` when None), timeout 30, and return `{"records": payload.get("records", []), "cursor": payload.get("cursor")}`.
    Create `apps/bluesky/reconcile.py` with function `def classify_record_operation(*, source_uri: str, source_cid: str) -> str` that queries `BlueskyPostMap` and returns one of `"created"`, `"updated"`, `"skipped"` by exact rules in behavior.
    Add `apps/bluesky/tests/test_reconcile.py` covering client call contract and operation classification with DB-backed fixtures.
    Do not publish or mutate `MicroPostPage` in this task.
  </action>
  <verify>
    <automated>make test ARGS='apps.bluesky.tests.test_reconcile'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "LIST_RECORDS_URL" apps/bluesky/client.py` returns one match.
    - `grep -n "collection\": \"app.bsky.feed.post\"" apps/bluesky/client.py` returns one match.
    - `grep -n "def classify_record_operation" apps/bluesky/reconcile.py` returns one match.
    - `grep -n "return \"skipped\"" apps/bluesky/reconcile.py` returns at least one match.
    - `grep -n "class TestClassifyRecordOperationContract" apps/bluesky/tests/test_reconcile.py` returns one match.
    - `make test ARGS='apps.bluesky.tests.test_reconcile'` exits 0.
  </acceptance_criteria>
  <done>Sync can fetch source records and deterministically classify created/updated/skipped operations for each source URI.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement run_sync orchestration and manual sync_bluesky command</name>
  <read_first>
    - apps/bluesky/sync.py
    - apps/bluesky/client.py
    - apps/bluesky/reconcile.py
    - apps/bluesky/publish.py
    - apps/bluesky/models.py
    - apps/bluesky/management/commands/sync_bluesky.py
    - apps/bluesky/tests/test_sync_bluesky_command.py
    - apps/gadgets/management/commands/sync_gadgets.py
  </read_first>
  <files>apps/bluesky/sync.py, apps/bluesky/management/__init__.py, apps/bluesky/management/commands/__init__.py, apps/bluesky/management/commands/sync_bluesky.py, apps/bluesky/tests/test_sync_bluesky_command.py</files>
  <behavior>
    - Test 1: `run_sync()` selects active and enabled `BlueskySourceSettings` and raises `ValueError("No active enabled Bluesky source configured")` when none exists.
    - Test 2: `run_sync()` iterates listRecords payload, calls `classify_record_operation(source_uri=..., source_cid=...)` before publish orchestration, and only calls `upsert_and_publish_micro_post(...)` for `"created"` or `"updated"` records.
    - Test 3: Running `run_sync()` twice with unchanged `source_cid` values keeps `MicroPostPage` count unchanged and increments `skipped`.
    - Test 4: `run_sync()` counter mapping is explicit and deterministic: classified `"created"` increments `imported`, classified `"updated"` increments `updated`, classified `"skipped"` increments `skipped`, and exceptions increment `failed`.
    - Test 5: `sync_bluesky` command prints `Sync complete: imported=<n> updated=<n> skipped=<n> failed=<n>` and forwards `--limit` argument.
  </behavior>
  <action>
    Create `apps/bluesky/sync.py` with function `def run_sync(*, limit: int = 100) -> dict[str, int]`.
    Use `BlueskySourceSettings.objects.filter(is_active=True, is_enabled=True).order_by("-updated_at").first()` as source selector.
    Fetch records via `list_feed_post_records(source_settings=source_settings, limit=limit)`.
    For each record read:
    - `uri` from record root
    - `cid` from record root
    - `indexedAt` from record root
    - `text` and `facets` from `record["value"]`
    - derive `source_rkey` from URI final segment after last `/`
    - call `classify_record_operation(source_uri=uri, source_cid=cid)` and branch on classification
    - for `"created"` and `"updated"`, pass values into `upsert_and_publish_micro_post(...)`
    - for `"skipped"`, do not call publish service and increment `skipped`
    Normalize operation contract between reconcile and publish: both must use `"created"`, `"updated"`, `"skipped"` values. Keep sync counters as `imported/updated/skipped/failed` by mapping `"created"` -> `imported`.
    Track counters in dict exactly shaped as `{"imported": 0, "updated": 0, "skipped": 0, "failed": 0}` and return final counts.
    Create command `apps/bluesky/management/commands/sync_bluesky.py` with `--limit` integer argument default `100`, call `run_sync(limit=...)`, and print success line with exact key order imported/updated/skipped/failed.
    Add command/orchestration tests in `apps/bluesky/tests/test_sync_bluesky_command.py` using `unittest.mock.patch` to assert `run_sync` call args and output format, and add assertions that `run_sync` invokes `classify_record_operation(...)` for each record while honoring the operation-to-counter mapping.
    Keep all orchestration in `run_sync`; command must stay thin and contain no business logic duplication.
  </action>
  <verify>
    <automated>make test ARGS='apps.bluesky.tests.test_sync_bluesky_command apps.bluesky.tests.test_reconcile apps.bluesky.tests.test_publish'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def run_sync" apps/bluesky/sync.py` returns one match.
    - `grep -n "No active enabled Bluesky source configured" apps/bluesky/sync.py` returns one match.
    - `grep -n "Sync complete: imported=" apps/bluesky/management/commands/sync_bluesky.py` returns one match.
    - `grep -n "classify_record_operation" apps/bluesky/sync.py` returns at least one match.
    - `grep -n "--limit" apps/bluesky/management/commands/sync_bluesky.py` returns at least one match.
    - `grep -n "created\"" apps/bluesky/tests/test_sync_bluesky_command.py` returns at least one match.
    - `grep -n "class TestSyncBlueskyCommand" apps/bluesky/tests/test_sync_bluesky_command.py` returns one match.
    - `make test ARGS='apps.bluesky.tests.test_sync_bluesky_command apps.bluesky.tests.test_reconcile apps.bluesky.tests.test_publish'` exits 0.
  </acceptance_criteria>
  <done>Manual command path imports new posts and safely skips unchanged posts using one deterministic run_sync orchestration path.</done>
</task>

</tasks>

<verification>
- Run `make test ARGS='apps.bluesky.tests'` for phase-app gate.
- Run `make test ARGS='apps.bluesky.tests.test_sync_bluesky_command apps.bluesky.tests.test_reconcile apps.bluesky.tests.test_transform apps.bluesky.tests.test_publish apps.bluesky.tests.test_post_map_model'` to verify requirement coverage.
</verification>

<success_criteria>
- `sync_bluesky` command imports new Bluesky posts into microblog entries through `run_sync`.
- A second sync run with unchanged source records creates no duplicate micro posts.
- Sync output counters make import outcomes explicit for operations.
</success_criteria>

<output>
After completion, create `.planning/phases/02-deterministic-import-and-auto-publish/02-03-SUMMARY.md`
</output>
