---
phase: 02-deterministic-import-and-auto-publish
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - apps/bluesky/transform.py
  - apps/bluesky/publish.py
  - apps/bluesky/tests/test_transform.py
  - apps/bluesky/tests/test_publish.py
autonomous: true
requirements: [PUBL-01, PUBL-02, PUBL-03]
must_haves:
  truths:
    - "Imported microblog entries render Bluesky text content without dropping characters."
    - "Imported microblog entries preserve canonical link targets provided by Bluesky facets."
    - "Imported entries are live immediately after sync without manual moderation actions."
  artifacts:
    - path: "apps/bluesky/transform.py"
      provides: "Facet-aware Bluesky text to rich text conversion"
      contains: "def render_post_body_html"
    - path: "apps/bluesky/publish.py"
      provides: "Create or update and publish MicroPostPage entries"
      contains: "def upsert_and_publish_micro_post"
    - path: "apps/bluesky/tests/test_transform.py"
      provides: "Text and link fidelity contract tests"
      contains: "class TestRenderPostBodyHtmlContract"
    - path: "apps/bluesky/tests/test_publish.py"
      provides: "Auto-publish and dedupe publish contract tests"
      contains: "class TestUpsertAndPublishMicroPostContract"
  key_links:
    - from: "apps/bluesky/transform.py"
      to: "apps/content/models.py"
      via: "Rendered HTML fed into StreamField paragraph block"
      pattern: "paragraph"
    - from: "apps/bluesky/publish.py"
      to: "apps/content/management/commands/bootstrap_content.py"
      via: "Use Wagtail revision publish API"
      pattern: "save_revision\(\)\.publish\(\)"
---

<objective>
Implement text/link transformation and auto-publish behavior for imported Bluesky content.

Purpose: Deliver publishing fidelity before orchestration wiring so sync can trust stable content output.
Output: Transformation and publishing services with automated contract tests.
</objective>

<execution_context>
@/Users/tschaack/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/tschaack/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-deterministic-import-and-auto-publish/02-RESEARCH.md
@.planning/phases/02-deterministic-import-and-auto-publish/02-VALIDATION.md
@.planning/phases/02-deterministic-import-and-auto-publish/02-01-PLAN.md
@apps/content/models.py
@apps/content/management/commands/bootstrap_content.py
@apps/bluesky/models.py

<interfaces>
From `apps/content/management/commands/bootstrap_content.py`:
```python
def save_post(blog_index, post):
    blog_index.add_child(instance=post)
    post.save()
    revision = post.save_revision()
    revision.publish()
```

From `apps/content/models.py`:
```python
class MicroPostPage(BaseContentPage):
    date = models.DateField("Post date")
    body = StreamField([
        ("paragraph", blocks.RichTextBlock()),
        ("image", ImageChooserBlock()),
    ])
```

From `apps/bluesky/models.py` (from Plan 02-01):
```python
class BlueskyPostMap(BaseModel):
    source_uri = models.CharField(max_length=512, unique=True)
    source_cid = models.CharField(max_length=128)
    micro_post = models.ForeignKey("content.MicroPostPage", ...)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build facet-aware text and link transformation service</name>
  <read_first>
    - apps/content/models.py
    - apps/bluesky/tests/test_transform.py
    - apps/bluesky/transform.py
    - .planning/phases/02-deterministic-import-and-auto-publish/02-RESEARCH.md
  </read_first>
  <files>apps/bluesky/transform.py, apps/bluesky/tests/test_transform.py</files>
  <behavior>
    - Test 1: Plain post text `"hello world"` produces the same text in rendered HTML paragraph output.
    - Test 2: A `app.bsky.richtext.facet#link` feature with byte range maps to `<a href="<facet-uri>">` in output.
    - Test 3: UTF-8 multi-byte text (for example emoji) preserves non-link text boundaries when applying facet offsets.
    - Test 4: If `facets` are absent, output contains escaped plain text only and no anchor tags.
  </behavior>
  <action>
    Create `apps/bluesky/transform.py` with these exact functions:
    - `def render_post_body_html(*, text: str, facets: list[dict] | None) -> str`
    - `def build_micropost_stream_body(*, text: str, facets: list[dict] | None) -> list[dict]`
    Implement `render_post_body_html` to parse only `feature["$type"] == "app.bsky.richtext.facet#link"`, apply `byteStart` and `byteEnd` using UTF-8 byte offsets, escape non-link text, and emit canonical anchor tags using the facet `uri` value.
    Implement `build_micropost_stream_body` to return exactly `[ {"type": "paragraph", "value": "<rendered_html>"} ]`.
    Create `apps/bluesky/tests/test_transform.py` with deterministic fixtures covering plain text, single link facet, and UTF-8 edge case.
    Do not implement mention/tag rendering and do not parse deprecated `entities`; only `facets` link features are in scope.
  </action>
  <verify>
    <automated>make test ARGS='apps.bluesky.tests.test_transform'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def render_post_body_html" apps/bluesky/transform.py` returns one match.
    - `grep -n "app.bsky.richtext.facet#link" apps/bluesky/transform.py` returns at least one match.
    - `grep -n "def build_micropost_stream_body" apps/bluesky/transform.py` returns one match.
    - `grep -n "class TestRenderPostBodyHtmlContract" apps/bluesky/tests/test_transform.py` returns one match.
    - `make test ARGS='apps.bluesky.tests.test_transform'` exits 0.
  </acceptance_criteria>
  <done>Transformation output preserves Bluesky text and canonical links with tested UTF-8-safe facet handling for PUBL-01 and PUBL-02.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement deterministic micro post upsert and auto-publish service</name>
  <read_first>
    - apps/bluesky/models.py
    - apps/content/models.py
    - apps/content/management/commands/bootstrap_content.py
    - apps/bluesky/tests/test_publish.py
    - apps/bluesky/publish.py
    - apps/bluesky/transform.py
  </read_first>
  <files>apps/bluesky/publish.py, apps/bluesky/tests/test_publish.py</files>
  <behavior>
    - Test 1: When no map exists for `source_uri`, service creates a new `MicroPostPage`, creates a `BlueskyPostMap`, and returns `operation == "created"`.
    - Test 2: When map exists and incoming `source_cid` differs, service updates existing page body/date, updates map `source_cid`, and returns `operation == "updated"`.
    - Test 3: Every create/update path calls `save_revision().publish()` and resulting page has `live == True`.
    - Test 4: When map exists and incoming `source_cid` is unchanged, service returns `operation == "skipped"` without creating another `MicroPostPage`.
  </behavior>
  <action>
    Create `apps/bluesky/publish.py` with `def upsert_and_publish_micro_post(*, source_settings, source_uri: str, source_cid: str, source_did: str, source_rkey: str, source_indexed_at, post_text: str, post_facets: list[dict] | None) -> dict[str, str]`.
    Implement deterministic selector for parent page: `BlogIndexPage.objects.live().order_by("id").first()` and raise `ValueError("No live BlogIndexPage available for Bluesky import")` when absent.
    For create path: instantiate `MicroPostPage` with `slug=f"bluesky-{source_rkey}"`, `title=f"Bluesky {source_rkey}"`, `date=source_indexed_at.date()`, and `body=build_micropost_stream_body(...)`; add as child and publish via `save_revision().publish()`.
    For update path: load `micro_post` from `BlueskyPostMap`, update `date` and `body`, save revision, publish.
    For skip path: if existing map `source_cid` equals incoming `source_cid`, return skip without new page creation.
    Persist map updates with `source_cid`, `source_indexed_at`, and linked `micro_post` on create/update.
    Add `apps/bluesky/tests/test_publish.py` integration tests with database assertions for map count, page count, and `live` status.
  </action>
  <verify>
    <automated>make test ARGS='apps.bluesky.tests.test_publish'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def upsert_and_publish_micro_post" apps/bluesky/publish.py` returns one match.
    - `grep -n "No live BlogIndexPage available for Bluesky import" apps/bluesky/publish.py` returns one match.
    - `grep -n "save_revision().publish()" apps/bluesky/publish.py` returns at least one match.
    - `grep -n "operation == \"skipped\"" apps/bluesky/tests/test_publish.py` returns at least one match.
    - `make test ARGS='apps.bluesky.tests.test_publish'` exits 0.
  </acceptance_criteria>
  <done>Importer publish service creates/updates live micro posts with deterministic dedupe outcomes and no duplicate page creation for unchanged source posts.</done>
</task>

</tasks>

<verification>
- Run `make test ARGS='apps.bluesky.tests.test_transform apps.bluesky.tests.test_publish'`.
- Run `make test ARGS='apps.bluesky.tests.test_post_map_model apps.bluesky.tests.test_transform apps.bluesky.tests.test_publish'` as wave gate.
</verification>

<success_criteria>
- Bluesky text and links are transformed into micro post body content with canonical link targets.
- Imported micro posts publish immediately and remain live after create/update.
- Deduping by `source_uri` + `source_cid` is operational at publish layer.
</success_criteria>

<output>
After completion, create `.planning/phases/02-deterministic-import-and-auto-publish/02-02-SUMMARY.md`
</output>
