---
phase: 04-scheduled-sync-and-concurrency-safety
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/bluesky/tests/test_scheduled_sync.py
  - apps/bluesky/tests/test_sync_concurrency.py
  - apps/bluesky/models.py
  - apps/bluesky/migrations/0005_blueskysourcesettings_sync_lock_fields.py
  - apps/bluesky/sync.py
  - apps/bluesky/tasks.py
  - taylor_learns/settings.py
autonomous: true
requirements:
  - SYNC-03
  - LIFE-04
must_haves:
  truths:
    - "Scheduled Bluesky sync runs execute automatically without a manual management-command trigger."
    - "When one sync run is already active for the configured source, a second run is skipped instead of overlapping writes."
    - "Scheduled runs call the same run_sync orchestration path as manual runs, preserving import/update/remove behavior."
  artifacts:
    - path: "apps/bluesky/tasks.py"
      provides: "Celery task entrypoint for scheduled Bluesky sync"
      contains: "sync_bluesky_task"
    - path: "taylor_learns/settings.py"
      provides: "Environment-controlled periodic schedule registration for Bluesky sync"
      contains: "bluesky-sync"
    - path: "apps/bluesky/models.py"
      provides: "Persistent per-source lease lock fields"
      contains: "sync_lock_token"
    - path: "apps/bluesky/sync.py"
      provides: "Single-flight lock acquire/release around run_sync pipeline"
      contains: "transaction.atomic"
  key_links:
    - from: "taylor_learns/settings.py"
      to: "apps.bluesky.tasks.sync_bluesky_task"
      via: "SCHEDULED_TASKS entry consumed by bootstrap_celery_tasks"
      pattern: "bluesky-sync|apps.bluesky.tasks.sync_bluesky_task"
    - from: "apps/bluesky/tasks.py"
      to: "apps/bluesky/sync.py"
      via: "task delegates directly to run_sync"
      pattern: "run_sync"
    - from: "apps/bluesky/sync.py"
      to: "apps/bluesky/models.py"
      via: "atomic compare-and-set lease lock on source row"
      pattern: "sync_lock_expires_at|sync_lock_token"
---

<objective>
Add automated Bluesky sync scheduling and deterministic per-source overlap prevention so operators can trust recurring runs without manual triggering or concurrent write races.

Purpose: Satisfies SYNC-03 and LIFE-04 by reusing existing Celery beat bootstrap and keeping one single-flight `run_sync` path for manual + scheduled triggers.
Output: Scheduled task wiring, source lease lock persistence, concurrency-safe `run_sync`, and requirement-level tests.
</objective>

<execution_context>
@/Users/tschaack/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/tschaack/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-post-lifecycle-reconciliation-and-run-visibility/03-02-SUMMARY.md
@.planning/phases/04-scheduled-sync-and-concurrency-safety/04-RESEARCH.md
@apps/bluesky/sync.py
@apps/bluesky/models.py
@apps/web/management/commands/bootstrap_celery_tasks.py
@taylor_learns/settings.py

<interfaces>
From apps/bluesky/sync.py:
```python
def run_sync(*, limit: int = 100) -> dict[str, int]:
    ...
```

From apps/bluesky/management/commands/sync_bluesky.py:
```python
result = run_sync(limit=int(options.get("limit") or 100))
```

From taylor_learns/settings.py:
```python
SCHEDULED_TASKS = {
    "gadgets-hourly-sync": {
        "task": "apps.gadgets.tasks.sync_gadgets_task",
        "schedule": schedules.crontab(minute=0),
        "enabled": GADGETS_SYNC_ENABLED,
    },
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED tests for scheduled trigger parity and overlap prevention</name>
  <read_first>
    - apps/bluesky/tests/test_sync_bluesky_command.py
    - apps/bluesky/sync.py
    - apps/gadgets/tasks.py
    - taylor_learns/settings.py
  </read_first>
  <files>apps/bluesky/tests/test_scheduled_sync.py, apps/bluesky/tests/test_sync_concurrency.py</files>
  <acceptance_criteria>
    - `apps/bluesky/tests/test_scheduled_sync.py` asserts `sync_bluesky_task` returns `{"status": "disabled"}` when scheduling is disabled and delegates to `run_sync` when enabled.
    - `apps/bluesky/tests/test_sync_concurrency.py` asserts a pre-held source lock causes `run_sync` to skip without calling `list_feed_post_records`.
    - `apps/bluesky/tests/test_sync_concurrency.py` asserts lock fields are cleared in `finally` after a raised sync-path exception.
    - At least one new test fails before implementation.
  </acceptance_criteria>
  <action>
    Create `apps/bluesky/tests/test_scheduled_sync.py` with task-level contract tests using `override_settings` and `patch` so scheduled execution behavior is explicit and independent of command invocation. Create `apps/bluesky/tests/test_sync_concurrency.py` covering two lock invariants: (1) overlap run is skipped for same active source lease and (2) lock release occurs on exception path. Keep assertions aligned with the existing `run_sync` counter contract (`imported`, `updated`, `removed`, `skipped`, `failed`) so no new counter keys are introduced.
  </action>
  <verify>
    <automated>make test ARGS='apps.bluesky.tests.test_scheduled_sync --keepdb -x'</automated>
  </verify>
  <done>RED tests exist and encode SYNC-03/LIFE-04 behavior boundaries for both scheduled and overlap paths.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement per-source DB lease lock inside run_sync</name>
  <read_first>
    - apps/bluesky/models.py
    - apps/bluesky/sync.py
    - apps/bluesky/tests/test_sync_concurrency.py
    - .planning/phases/04-scheduled-sync-and-concurrency-safety/04-RESEARCH.md
  </read_first>
  <files>apps/bluesky/models.py, apps/bluesky/migrations/0005_blueskysourcesettings_sync_lock_fields.py, apps/bluesky/sync.py</files>
  <acceptance_criteria>
    - `BlueskySourceSettings` has nullable lock lease fields (`sync_lock_token`, `sync_lock_expires_at`) with migration `0005_blueskysourcesettings_sync_lock_fields.py`.
    - `run_sync` acquires lock via atomic compare-and-set update scoped to active source and returns deterministic overlap skip result without processing records when lock is held.
    - `run_sync` always releases lock in `finally` only when token owner matches, including exception paths.
  </acceptance_criteria>
  <action>
    Add two nullable fields on `BlueskySourceSettings`: `sync_lock_token` (UUID/char token) and `sync_lock_expires_at` (datetime lease boundary), then add migration `0005_blueskysourcesettings_sync_lock_fields.py`. In `apps/bluesky/sync.py`, acquire lock before remote fetch with `transaction.atomic()` + filtered `update(...)` allowing acquire only when lease is null/expired; generate a unique token and short lease window (for example 10 minutes) per run. If acquire fails, return counters with `skipped=1` and zero other counts, and persist run row using the existing counter schema for visibility. Wrap sync body in `try/finally` and release by updating lock fields to null only when `id` + `sync_lock_token` match.
  </action>
  <verify>
    <automated>make test ARGS='apps.bluesky.tests.test_sync_concurrency --keepdb -x'</automated>
  </verify>
  <done>run_sync is single-flight per source with lease-based overlap skip and guaranteed owner-safe lock release.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire periodic Bluesky task into scheduler configuration</name>
  <read_first>
    - taylor_learns/settings.py
    - apps/web/management/commands/bootstrap_celery_tasks.py
    - apps/gadgets/tasks.py
    - apps/bluesky/tests/test_scheduled_sync.py
  </read_first>
  <files>apps/bluesky/tasks.py, taylor_learns/settings.py, apps/bluesky/tests/test_scheduled_sync.py</files>
  <acceptance_criteria>
    - `apps/bluesky/tasks.py` defines `@shared_task sync_bluesky_task` that gates on a dedicated enable flag and otherwise calls `run_sync(limit=100)`.
    - `taylor_learns/settings.py` defines `BLUESKY_SYNC_ENABLED` and adds a `bluesky-sync` entry in `SCHEDULED_TASKS` pointing to `apps.bluesky.tasks.sync_bluesky_task` with recurring crontab cadence.
    - `make manage ARGS='bootstrap_celery_tasks'` can materialize the new periodic task entry without custom command changes.
  </acceptance_criteria>
  <action>
    Create `apps/bluesky/tasks.py` following the established gadget task pattern: return `{"status": "disabled"}` when `BLUESKY_SYNC_ENABLED` is false, otherwise call `run_sync(limit=100)`. In `taylor_learns/settings.py`, add `BLUESKY_SYNC_ENABLED = env.bool("BLUESKY_SYNC_ENABLED", default=False)` near other sync flags and register `"bluesky-sync"` under `SCHEDULED_TASKS` with `schedules.crontab(minute="*/15")`, enabled by that flag. Keep task registration fully declarative so `bootstrap_celery_tasks` picks it up unchanged.
  </action>
  <verify>
    <automated>make test ARGS='apps.bluesky.tests.test_scheduled_sync --keepdb -x'</automated>
  </verify>
  <done>Scheduled Bluesky sync is configured and executable through existing Celery beat bootstrap workflow with parity to manual run_sync behavior.</done>
</task>

</tasks>

<verification>
- `make test ARGS='apps.bluesky.tests.test_scheduled_sync apps.bluesky.tests.test_sync_concurrency -x'` (wave gate)
- `make test ARGS='apps.bluesky.tests.test_sync_bluesky_command apps.bluesky.tests.test_sync_run_visibility -x'` (phase gate)
- `make manage ARGS='bootstrap_celery_tasks'`
</verification>

<success_criteria>
- Celery beat configuration includes an environment-gated Bluesky periodic task that runs without manual command invocation.
- A second concurrent `run_sync` attempt for the same source does not process records and instead exits with deterministic skip behavior.
- Scheduled and manual triggers share one orchestration path (`run_sync`) and preserve existing import/update/remove counter semantics.
</success_criteria>

<output>
After completion, create `.planning/phases/04-scheduled-sync-and-concurrency-safety/04-01-SUMMARY.md`
</output>
