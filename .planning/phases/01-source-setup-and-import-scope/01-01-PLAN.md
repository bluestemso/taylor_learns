---
phase: 01-source-setup-and-import-scope
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/bluesky/__init__.py
  - apps/bluesky/apps.py
  - apps/bluesky/models.py
  - apps/bluesky/services/__init__.py
  - apps/bluesky/services/identity.py
  - apps/bluesky/tests/__init__.py
  - apps/bluesky/tests/test_settings_model.py
  - apps/bluesky/tests/test_identity_service.py
autonomous: true
requirements: [SYNC-01, SYNC-05]
must_haves:
  truths:
    - "An operator can enter a handle and then see the configured source as canonical handle plus DID."
    - "Operators cannot end up with multiple active Bluesky sources at once."
    - "Operators must provide a backfill start date and can use an effective timezone-boundary value for sync."
  artifacts:
    - path: "apps/bluesky/models.py"
      provides: "Bluesky source settings model with singleton and backfill contract"
      contains: "class BlueskySourceSettings(BaseModel)"
    - path: "apps/bluesky/services/identity.py"
      provides: "Handle verification and canonical identity resolution"
      contains: "def resolve_handle_identity(handle: str)"
    - path: "apps/bluesky/migrations/0001_initial.py"
      provides: "Database enforcement for source settings contract"
      contains: "unique_active_bluesky_source"
  key_links:
    - from: "apps/bluesky/models.py"
      to: "taylor_learns/settings.py"
      via: "timezone-aware backfill boundary conversion"
      pattern: "ZoneInfo\(settings.TIME_ZONE\)"
    - from: "apps/bluesky/tests/test_identity_service.py"
      to: "apps/bluesky/services/identity.py"
      via: "mocked httpx response contract"
      pattern: "resolve_handle_identity"
---

<objective>
Define the Phase 1 data and verification contract for the single Bluesky source configuration.

Purpose: Establish durable, deterministic source settings that later sync phases can trust.
Output: New `apps/bluesky` app skeleton with model, identity service, and contract tests.
</objective>

<execution_context>
@/Users/tschaack/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/tschaack/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-source-setup-and-import-scope/01-CONTEXT.md
@.planning/phases/01-source-setup-and-import-scope/01-RESEARCH.md
@apps/gadgets/models.py
@apps/gadgets/sync.py
@apps/utils/models.py
@taylor_learns/settings.py

<interfaces>
From `apps/utils/models.py`:
```python
class BaseModel(models.Model):
    created_at = models.DateTimeField(auto_now_add=True)
    updated_at = models.DateTimeField(auto_now=True)
```

From `apps/gadgets/models.py`:
```python
class GadgetSyncStatus(models.TextChoices):
    NEVER = "never", _("Never")
    SUCCESS = "success", _("Success")
    SKIPPED = "skipped", _("Skipped")
    ERROR = "error", _("Error")
```

From `taylor_learns/settings.py`:
```python
TIME_ZONE = "UTC"
PROJECT_APPS = [
    "apps.content",
    ...,
    "apps.gadgets.apps.GadgetsConfig",
]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Bluesky source settings model and identity resolver contract</name>
  <read_first>
    - apps/utils/models.py
    - apps/gadgets/models.py
    - apps/gadgets/sync.py
    - .planning/phases/01-source-setup-and-import-scope/01-CONTEXT.md
    - .planning/phases/01-source-setup-and-import-scope/01-RESEARCH.md
  </read_first>
  <files>apps/bluesky/__init__.py, apps/bluesky/apps.py, apps/bluesky/models.py, apps/bluesky/services/__init__.py, apps/bluesky/services/identity.py, apps/bluesky/tests/__init__.py, apps/bluesky/tests/test_settings_model.py, apps/bluesky/tests/test_identity_service.py</files>
  <behavior>
    - Test 1: `resolve_handle_identity("taylorlearns.com")` calls `https://public.api.bsky.app/xrpc/com.atproto.identity.resolveHandle` with `params={"handle": "taylorlearns.com"}` and returns canonical values containing a `did:` identifier.
    - Test 2: resolver raises `ValidationError` when response has no `did` key, non-2xx status, or timeout/network failure.
    - Test 3: `BlueskySourceSettings` enforces a single active row with DB constraint name `unique_active_bluesky_source`.
    - Test 4: `backfill_start_date` is required and `effective_backfill_start_at()` returns site-timezone midnight (`00:00:00`) using `ZoneInfo(settings.TIME_ZONE)`.
  </behavior>
  <action>
    Create `apps/bluesky` app code with exact contracts:
    1) `apps/bluesky/apps.py` defines `class BlueskyConfig(AppConfig)` with `name = "apps.bluesky"` and `label = "bluesky"`.
    2) `apps/bluesky/models.py` defines:
       - `class BlueskySourceSettings(BaseModel)` with fields:
         - `handle = models.CharField(max_length=253)`
         - `did = models.CharField(max_length=255)`
         - `profile_url = models.URLField()`
         - `backfill_start_date = models.DateField()`
         - `is_enabled = models.BooleanField(default=True)`
         - `is_active = models.BooleanField(default=True)`
         - `verified_at = models.DateTimeField(blank=True, null=True)`
       - `class Meta` includes `UniqueConstraint(fields=["is_active"], condition=Q(is_active=True), name="unique_active_bluesky_source")` and `ordering = ["-updated_at"]`.
       - method `effective_backfill_start_at(self) -> datetime` returning timezone-aware midnight for `backfill_start_date` in `settings.TIME_ZONE`.
    3) `apps/bluesky/services/identity.py` defines `resolve_handle_identity(handle: str) -> dict[str, str]` that:
       - Sends `httpx.get("https://public.api.bsky.app/xrpc/com.atproto.identity.resolveHandle", params={"handle": handle}, timeout=10)`.
       - Accepts `did` from JSON, optional `handle` from JSON fallback to submitted handle.
       - Returns dict with keys `handle`, `did`, and `profile_url` where `profile_url` is `https://bsky.app/profile/<resolved_handle_or_did>`.
       - Raises `django.core.exceptions.ValidationError` with a user-facing message if endpoint/network/contract validation fails.
    4) Create tests in `apps/bluesky/tests/test_settings_model.py` and `apps/bluesky/tests/test_identity_service.py` that assert these exact contracts and failure modes.
    Do not implement admin form behavior or replace-confirm logic in this task.
  </action>
  <verify>
    <automated>make test ARGS='apps.bluesky.tests.test_settings_model.TestBlueskySourceSettingsContract apps.bluesky.tests.test_identity_service.TestResolveHandleIdentityContract'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "class BlueskySourceSettings" apps/bluesky/models.py` returns one match.
    - `grep -n "unique_active_bluesky_source" apps/bluesky/models.py` returns one match.
    - `grep -n "ZoneInfo(settings.TIME_ZONE)" apps/bluesky/models.py` returns one match.
    - `grep -n "com.atproto.identity.resolveHandle" apps/bluesky/services/identity.py` returns one match.
    - `make test ARGS='apps.bluesky.tests.test_settings_model.TestBlueskySourceSettingsContract apps.bluesky.tests.test_identity_service.TestResolveHandleIdentityContract'` exits 0.
  </acceptance_criteria>
  <done>Model and identity contracts are implemented and enforced by automated tests for SYNC-01 and SYNC-05 foundations.</done>
</task>

</tasks>

<verification>
- Task-level smoke gate: run `make test ARGS='apps.bluesky.tests.test_settings_model.TestBlueskySourceSettingsContract apps.bluesky.tests.test_identity_service.TestResolveHandleIdentityContract'`.
- Wave gate (broader): run `make test ARGS='apps.bluesky.tests.test_settings_model apps.bluesky.tests.test_identity_service'`.
</verification>

<success_criteria>
- `BlueskySourceSettings` exists with required backfill date and DB-enforced single active source behavior.
- Handle verification service resolves canonical DID/profile values and fails safely on invalid responses.
- Source-settings model and identity resolver contracts are ready for migration and admin wiring plans.
</success_criteria>

<output>
After completion, create `.planning/phases/01-source-setup-and-import-scope/01-01-SUMMARY.md`
</output>
