---
phase: 01-source-setup-and-import-scope
plan: 02
type: execute
wave: 3
depends_on: [01-01, 01-03]
files_modified:
  - apps/bluesky/forms.py
  - apps/bluesky/admin.py
  - apps/bluesky/tests/test_admin_source_settings.py
  - apps/bluesky/tests/test_settings_model.py
autonomous: true
requirements: [SYNC-01, SYNC-05]
must_haves:
  truths:
    - "A superuser can configure one verified Bluesky source in Django admin using a guided form."
    - "Replacing an existing source requires explicit confirmation before save."
    - "Admin surfaces show the active handle, DID, profile link, backfill date, and enabled state used by sync."
  artifacts:
    - path: "apps/bluesky/forms.py"
      provides: "Guided admin validation for source verification and replace confirmation"
      contains: "confirm_replace"
    - path: "apps/bluesky/admin.py"
      provides: "Admin list/detail views and superuser-only write permissions"
      contains: "effective_settings"
    - path: "apps/bluesky/tests/test_admin_source_settings.py"
      provides: "Executable checks for admin setup and visibility behavior"
      contains: "class TestBlueskySourceSettingsAdmin"
  key_links:
    - from: "apps/bluesky/forms.py"
      to: "apps/bluesky/services/identity.py"
      via: "save-time verification call"
      pattern: "resolve_handle_identity"
    - from: "apps/bluesky/admin.py"
      to: "apps/bluesky/models.py"
      via: "readonly effective settings projection"
      pattern: "effective_settings"
---

<objective>
Deliver the Phase 1 operator UX in Django admin for verified single-source setup and backfill visibility.

Purpose: Ensure configuration is safe, explicit, and inspectable before any import execution phase begins.
Output: Guided admin form + list/detail visibility + tests enforcing replacement confirmation and verification gates.
</objective>

<execution_context>
@/Users/tschaack/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/tschaack/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-source-setup-and-import-scope/01-CONTEXT.md
@.planning/phases/01-source-setup-and-import-scope/01-RESEARCH.md
@apps/gadgets/admin.py
@apps/bluesky/models.py
@apps/bluesky/services/identity.py

<interfaces>
From `apps/gadgets/admin.py`:
```python
@admin.register(GadgetSource)
class GadgetSourceAdmin(admin.ModelAdmin):
    list_display = (...)
    readonly_fields = (...)
```

From `apps/bluesky/services/identity.py` (created in plan 01):
```python
def resolve_handle_identity(handle: str) -> dict[str, str]:
    ...
```

From `apps/bluesky/models.py` (created in plan 01):
```python
class BlueskySourceSettings(BaseModel):
    handle = models.CharField(max_length=253)
    did = models.CharField(max_length=255)
    profile_url = models.URLField()
    backfill_start_date = models.DateField()
    is_enabled = models.BooleanField(default=True)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build guided admin form with verification-before-save and replace confirmation gate</name>
  <read_first>
    - apps/bluesky/models.py
    - apps/bluesky/services/identity.py
    - apps/gadgets/admin.py
    - .planning/phases/01-source-setup-and-import-scope/01-CONTEXT.md
  </read_first>
  <files>apps/bluesky/forms.py, apps/bluesky/admin.py, apps/bluesky/tests/test_admin_source_settings.py</files>
  <behavior>
    - Test 1: superuser POST with `handle='taylorlearns.com'`, `backfill_start_date='2024-01-01'`, `is_enabled=True` resolves identity and persists canonical `handle`, `did`, and `profile_url`.
    - Test 2: verification failure (non-2xx/invalid payload) returns form error and no row is saved.
    - Test 3: when an active row exists and submitted identity changes, save is blocked unless `confirm_replace=True`.
    - Test 4: non-superusers cannot add/change source settings in admin.
  </behavior>
  <action>
    Implement guided admin setup flow with concrete behavior:
    1) Create `apps/bluesky/forms.py` with `class BlueskySourceSettingsAdminForm(forms.ModelForm)` and extra field `confirm_replace = forms.BooleanField(required=False, label="Confirm source replacement")`.
    2) In form clean/save logic:
       - Call `resolve_handle_identity(handle)` before persistence.
       - Set `cleaned_data["did"]`, `cleaned_data["handle"]`, and `cleaned_data["profile_url"]` from resolved values.
       - Query current active settings (`BlueskySourceSettings.objects.filter(is_active=True).exclude(pk=self.instance.pk).first()`).
       - If active row exists and resolved `did` differs from existing active `did`, require `confirm_replace` or raise `forms.ValidationError("Confirm source replacement to change the active Bluesky source.")`.
    3) Create `apps/bluesky/admin.py` and register `BlueskySourceSettings` using this form.
    4) Enforce superuser-only writes by overriding `has_add_permission`, `has_change_permission`, and `has_delete_permission` so only `request.user.is_superuser` may add/change; keep delete disabled (`False`) to avoid bypassing replacement guard.
    5) Add tests in `apps/bluesky/tests/test_admin_source_settings.py` that patch `apps.bluesky.forms.resolve_handle_identity` and assert each behavior above.
    Do not add settings history/audit log fields or models.
  </action>
  <verify>
    <automated>make test ARGS='apps.bluesky.tests.test_admin_source_settings.TestBlueskySourceSettingsAdminFlow'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "confirm_replace" apps/bluesky/forms.py` returns at least 2 matches (field + validation use).
    - `grep -n "resolve_handle_identity" apps/bluesky/forms.py` returns one match.
    - `grep -n "Confirm source replacement to change the active Bluesky source" apps/bluesky/forms.py` returns one match.
    - `grep -n "def has_add_permission" apps/bluesky/admin.py` returns one match.
    - `make test ARGS='apps.bluesky.tests.test_admin_source_settings.TestBlueskySourceSettingsAdminFlow'` exits 0.
  </acceptance_criteria>
  <done>Admin save path verifies source identity before save and blocks unconfirmed source replacement, satisfying SYNC-01 guardrails.</done>
</task>

<task type="auto">
  <name>Task 2: Expose required admin summary/detail effective settings visibility</name>
  <read_first>
    - apps/bluesky/admin.py
    - apps/bluesky/models.py
    - apps/gadgets/admin.py
    - .planning/phases/01-source-setup-and-import-scope/01-CONTEXT.md
  </read_first>
  <files>apps/bluesky/admin.py, apps/bluesky/tests/test_admin_source_settings.py, apps/bluesky/tests/test_settings_model.py</files>
  <action>
    Add required operator visibility surfaces:
    1) In `apps/bluesky/admin.py`, set `list_display = ("handle", "backfill_start_date", "is_enabled", "did", "verified_at", "updated_at")` so summary always includes handle, backfill date, and enabled state.
    2) Add read-only detail fields:
       - `readonly_fields` must include `"did"`, `"profile_url"`, `"verified_at"`, and a computed `"effective_settings"` block.
       - Implement `@admin.display(description="Effective settings") def effective_settings(self, obj)` returning HTML lines with exact labels:
         - `Handle: <value>`
         - `DID: <value>`
         - `Profile: <value>`
         - `Backfill from: <YYYY-MM-DD 00:00:00 TIME_ZONE>`
         - `Enabled: <True/False>`
    3) Group form fields into one guided edit surface using `fieldsets` with one editable section (`handle`, `backfill_start_date`, `is_enabled`, `confirm_replace`) and one read-only section (`did`, `profile_url`, `verified_at`, `effective_settings`).
    4) Expand tests to assert changelist columns and detail page include required values and labels above.
    5) Keep this phase admin-only: do not add public views, API endpoints, or management command flags for these settings.
  </action>
  <verify>
    <automated>make test ARGS='apps.bluesky.tests.test_admin_source_settings.TestBlueskySourceSettingsAdminVisibility'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "list_display =" apps/bluesky/admin.py` includes `handle`, `backfill_start_date`, and `is_enabled`.
    - `grep -n "@admin.display(description=\"Effective settings\")" apps/bluesky/admin.py` returns one match.
    - `grep -n "Backfill from:" apps/bluesky/admin.py` returns one match.
    - `grep -n "Enabled:" apps/bluesky/admin.py` returns one match.
    - `make test ARGS='apps.bluesky.tests.test_admin_source_settings.TestBlueskySourceSettingsAdminVisibility'` exits 0.
  </acceptance_criteria>
  <done>Admin list/detail surfaces show required SYNC-05 configuration state and effective values used by future sync runs.</done>
</task>

</tasks>

<verification>
- Task-level smoke gate: run `make test ARGS='apps.bluesky.tests.test_admin_source_settings.TestBlueskySourceSettingsAdminFlow'` then `make test ARGS='apps.bluesky.tests.test_admin_source_settings.TestBlueskySourceSettingsAdminVisibility'`.
- Wave gate (broader): run `make test ARGS='apps.bluesky.tests.test_admin_source_settings apps.bluesky.tests.test_settings_model'`.
- Spot-check admin UI with test client assertions included in automated tests (no manual checkpoint required).
</verification>

<success_criteria>
- Superusers can save only verified source settings from a guided admin form.
- Source replacement cannot occur without explicit confirmation.
- Admin summary and detail views display handle, DID, profile link, backfill date, enabled state, and effective settings.
</success_criteria>

<output>
After completion, create `.planning/phases/01-source-setup-and-import-scope/01-02-SUMMARY.md`
</output>
